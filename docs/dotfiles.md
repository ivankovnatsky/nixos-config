# Dotfiles

When setting up a new machine, remember to initialize a Git repository in your
home directory (`~`) to track files you want to preserve during system
maintenance, such as `brew uninstall` which might clean up config directories.
