# This is a placeholder for Kustomize build-time replacements
# The actual secret is managed by sealed-secrets
apiVersion: v1
kind: Secret
metadata:
  name: openwebui-config
  namespace: openwebui
stringData:
  host: openwebui.example.com  # Placeholder - will be overridden by sealed-secrets
  external-domain: example.com  # Placeholder - will be overridden by sealed-secrets