# Statix configuration
# https://git.peppe.rs/languages/statix/about/

disabled = [
  # Keep { ... }: pattern to match nixpkgs convention
  "empty_pattern",
  # Preserve == true/false for polymorphic type checks
  "bool_comparison",
]
